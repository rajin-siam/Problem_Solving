#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t,i,counter=0;
    scanf("%d",&t);
    cin.ignore();
    while(t--)
    {
        char s[100];
        fgets(s,100,stdin);
        int l=strlen(s);
        for(i=l-1;i>=0;i--)printf("%c",s[i]);
        printf("\n");
    }
}

