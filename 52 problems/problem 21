#include<bits/stdc++.h>
using namespace std;

int main()
{
	int n,i,t,ounter=0,l;
	char s[1001];
	scanf("%d",&t);
	while(t--)
    {
        scanf("%s",s);
        n=strlen(s);
        for(i=n-1;i>=0;i--)
        {
            printf("%c",s[i]);
        }
        printf("\n");
    }
}
